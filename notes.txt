


добавлен путь для сервиса, принимающий геометрию в качестве параметра

@app.route('/parcelgeom/<parcel_geom>', methods=['GET','POST'])
...

к нему прототип шаблона
parcel_geom.html


в виджете конфигурация заявлена, но пока параметры не имплементированы


в исходном тексте нужно явно указать точку входа для вашего сервиса


Widget.js

строка 206

showPopUp('http://адрес вашего сервиса:5020/parcelgeom/' + geojson0);

не могу понять, почему объект рисования теряет зону видимости, не могу деактивировать после того как закончил рисование. может ктото глянет из ваших скриптовиков
строка 184 //this.drawtoolbar.deactivate(); //  null ? 


бизнес процесс такой:
- при нажатии на кнопку обводим участок
- при двойном клике "закончить рисование" - формируется geojson, который дальше передается вашему сервису во всплывающем окне браузера, 
	дальше работает логика вашего сервиса


